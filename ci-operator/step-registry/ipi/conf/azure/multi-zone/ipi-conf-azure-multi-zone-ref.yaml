ref:
  as: ipi-conf-azure-multi-zone
  from_image:
    namespace: ocp
    name: "4.12"
    tag: upi-installer
  commands: ipi-conf-azure-multi-zone-commands.sh
  resources:
    requests:
      cpu: 10m
      memory: 100Mi
  documentation: |-
    The IPI Azure alters the install config to add new subnets and NAT gateways for the subnet.
